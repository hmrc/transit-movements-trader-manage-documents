@(controlResult: viewmodels.ControlResult, principal: viewmodels.Principal, authorisationId: Option[String])

@if(controlResult.isPrintable) {
    <fo:block>
        @authorisationId.map(x => x)
    </fo:block>
    <fo:block>
        @principal.name
    </fo:block>
} else {
    <fo:block/>
}
