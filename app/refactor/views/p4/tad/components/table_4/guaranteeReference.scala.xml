@import refactor.viewmodels.p4.tad.Table4ViewModel.GuaranteeViewModel

@(guarantees: Seq[GuaranteeViewModel])

@for(i <- 0 to 2){
    @if(guarantees.isDefinedAt(i)){
        <fo:table-cell display-align="center" padding-left="1mm" text-align="left" font-size="6pt" padding-top="0.5mm">
            <fo:block>
                @guarantees(i).ref
                @guarantees(i).otherRef
                @if(i == 2 && guarantees.length > 3){
                    ***
                }
            </fo:block>
        </fo:table-cell>
    } else {
        <fo:table-cell>
            <fo:block/>
        </fo:table-cell>
    }
}
